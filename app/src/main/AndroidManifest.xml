<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.artapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".specificchat.SpecificChatActivity">

        </activity>
        <activity android:name=".profileothers.ProfileOthersActivity">

        </activity>
        <activity
            android:name=".principal.PaginaPrincipalActivity"/>
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".chat.ChatActivity">

        </activity>
        <activity android:name=".informationspecific.InformationSpecificActivity" />
        <activity android:name=".profile.ProfileActivity" />
        <activity android:name=".categories.CategoryActivity" />
    </application>

</manifest>